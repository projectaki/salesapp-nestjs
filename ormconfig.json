{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "postgres",
  "password": "1234",
  "database": "salesapp",
  "entities": ["dist/**/*.model.{js,ts}"],
  "synchronize": false,
  "migrations": ["dist/migration/*.{js,ts}"],
  "cli": {
    "migrationsDir": "migration"
  }
}
